우선 각각의 이름,공격력,형태,공속을 입력하고 서로의 공격력과,형태,공속의 합을 비교해 더 큰쪽의 이름을
출력한다.


```java
package Lecture20210523;

import javax.sound.midi.Soundbank;
import java.util.Scanner;

import static Lecture20210523.information.plus;

public class Main {

    public static void main(String[] args) {

        Scanner scan=new Scanner(System.in);
        System.out.println("A의 이름을 입력하시오.");
        String Aname = scan.nextLine();
        System.out.println("B의 이름을 입력하시오.");
        String Bname = scan.nextLine();
        System.out.println("A의 공격력을 입력하시오.");
        int Aattack=scan.nextInt();
        System.out.println("B의 공격력을 입력하시오.");
        int Battack=scan.nextInt();
        System.out.println("A의 형태를 입력하시오.(원거리면 1,근거리면 0을 입력하시오.)");
        int Aform=scan.nextInt();
        System.out.println("B의 형태를 입력하시오.(원거리면 1,근거리면 0을 입력하시오.)");
        int Bform=scan.nextInt();
        System.out.println("A의 공격속도를 입력하시오.");
        int Aspeed=scan.nextInt();
        System.out.println("B의 공격속도를 입력하시오.");
        int Bspeed=scan.nextInt();

        information A = new information(Aname,Aattack,Aform,Aspeed);
        information B = new information(Bname,Battack,Bform,Bspeed);

        int Asum = plus(Aattack,Aform,Aspeed);
        int Bsum = plus(Battack,Bform,Bspeed);

        if(Asum>Bsum){
            System.out.println(Aname);
        }

       else if(Bsum>Asum){
            System.out.println(Bname);
        }


    }


    }

    class information{

        private int Acount=0;
        private int Bcount=0;

        private String name=null;
        private int attack;
        private int form;
        private int speed;


        public information() {}

        public information(String name,int attack,int form,int speed) {

            this.name = name;
            this.attack = attack;
            this.form = form;
            this.speed = speed;
        }

      public static int plus(int a,int f,int s){
        return a+f+s;
      }

    }
```

입력:
a
b
10
11
0
1
11
12

출력:b
